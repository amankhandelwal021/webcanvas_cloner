// This file is just a re-export from the projects directory
// to maintain backward compatibility with existing imports
import { getProjects, getProjectBySlug, getProjectById } from './projects';

export { getProjects, getProjectBySlug, getProjectById }; 